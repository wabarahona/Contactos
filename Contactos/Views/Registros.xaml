<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="Contactos.Views.Registros">
    <ContentPage.Content>
    <StackLayout>
        <CollectionView x:Name="ListaRegistros"
                        Margin=" 20,0,0,0"
                        SelectionMode="Single"
                        SelectionChanged="ListaRegistros_SelectionChanged">

            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="10"/>
            </CollectionView.ItemsLayout>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout>
                        <Label Text="{Binding ID}" />
                        <Label Text="{Binding Nombre}" />
                        <Label Text="{Binding Apellido}" />
                        <Label Text="{Binding Edad}" />
                        <Label Text="{Binding Pais}" />
                        <Label Text="{Binding Nota}" />
                        <Label Text="{Binding Longitud}" />
                        <Label Text="{Binding Latitud}" />

                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>


        </CollectionView>
    </StackLayout>        
    </ContentPage.Content>
</ContentPage>
